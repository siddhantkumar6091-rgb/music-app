workflows:
  android-workflow:
    name: Build APK

    environment:
      flutter: stable

    scripts:
      - flutter clean
      - rm -rf ~/.pub-cache
      - flutter pub get
      - flutter build apk --release

    artifacts:
      - build/app/outputs/flutter-apk/app-release.apk