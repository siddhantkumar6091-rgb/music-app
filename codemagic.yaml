workflows:
  android-workflow:
    name: Build APK

    environment:
      flutter: stable

    scripts:
      - flutter clean || true
      - flutter pub get
      - flutter build apk --release

    artifacts:
      - build/app/outputs/flutter-apk/app-release.apk